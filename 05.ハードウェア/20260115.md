<!-- markdownlint-disable md025 -->
# 20260115

# 論理回路

AND 回路がカマボコ  
OR 回路がへっこみ（一生覚えられんコレ

半加算器と全加算器  
※図  

半加算器：XORとAND  
全加算器：半加算器2個とOR  
z(和) = (x⊕y)⊕c  
c'(桁上げ) = x*y + (x⊕y)*c  

NAND 回路
X AND Y = (X NAND Y ) NAND (X NAND Y)  
X OR Y =  (X NAND X ) NAND (Y NAND Y)  
NOT X = X NAND X  
X XOR Y = ( X NAND (X NAND Y)) NAND (( X NAND Y ) NAND Y )  

フリップフロップ（FF）：1ビットの情報を一時的に保存できる、SRAMに使われる  
3入力多数決回路  

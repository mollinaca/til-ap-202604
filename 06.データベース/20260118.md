<!-- markdownlint-disable md025 -->

# 20260118

# 表の定義

* CREATE TABLE 構文
* 列制約
  * 一意制約（PRIMARY KEY, UNIQUE）
  * 非null制約
  * 参照制約 REFERENCES
* 行制約
  * PRIMARY KEY
  * FOREIGN KEY

# 関係代数とSELECT文

* UNION 和
* INTERSECT 共通
* EXCEPT 差

* 関係演算
  * 選択 SELECT WHERE
  * 射影 SELECT
  * 結合 JOIN
    * INNER JOIN
    * OUTER JOIN
      * [LEFT|RIGHT|FULL]
